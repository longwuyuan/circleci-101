version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:current
    resource_class: longwuyuan/k8s
    steps:
      - checkout
      - run: echo "Hi I'm on Runners!"

workflows:
  build-workflow:
    jobs:
      - build